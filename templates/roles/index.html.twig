{% extends 'accueil.html.twig' %}

{% block title %}Rôles{% endblock %}

{% block body %}
    <div class="container col-md-6">
        <div class="card bg-transparent">
            <div class="card-header">Liste des Rôles</div>
            <div class="card-body">
                <table class="table table-striped">
                    <tr>
                        <th>Identifiant</th>
                        <th>Nom Rôle</th>
                        <th>Editer</th>
                        <th>Supprimer</th>
                    </tr>
                    {% for r in roles %}
                        <tr>
                            <td>{{ r.id }}</td>
                            <td>{{ r.name }}</td>
                            <td><a class="btn btn-dark" href="{{ path('edit_role',{'id':r.id} ) }}"><i class="fa fa-edit"></i></a></td>
                            <td><a class="btn btn-danger" href="{{ path('delete_role',{'id':r.id} ) }}"><i class="fa fa-trash"></i></a></td>
                        </tr>
                    {%endfor%}
                </table>
            </div>
        </div>
    </div>
    <div class="container col-md-6">
        <div class="card">
            <div class="card-header">Formulaire de gestion des Rôles</div>
            <div class="card-body">
                {{ form(form) }}
            </div>
        </div>
    </div>
{% endblock %}
{% block footer %}

{% endblock %}